```bash
uv venv
source .venv/bin/activate
uv pip install -e . -i https://mirrors.aliyun.com/pypi/simple
uv pip compile -i https://mirrors.aliyun.com/pypi/simple requirements.txt -o pylock.toml --format=pylock.toml
```