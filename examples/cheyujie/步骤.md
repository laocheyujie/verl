# 步骤

## 环境搭建

```bash

```

## 数据

```json
{"query": "回答下面的单项选择题，请选出其中的正确答案。你的回答的最后一行应该是这样的格式：\"答案：LETTER\"（不带引号），其中 LETTER 是 A,B,C,D 中的一个。\n\n\n问题：XXX，下列说法正确的是？\n选项：\nA) xxx\nB) xxx\nC) xxx\nD) xxx", "response": "C"}
{"query": "回答下面的多项选择题，可能有多个正确答案，请选出其中的正确答案。你的回答的最后一行应该是这样的格式：\"答案：LETTERS\"（不带引号），其中 LETTERS 是 A,B,C,D 中的一个或多个。\n\n问题：XXX，下列说法正确的是？\n选项：\nA) xxx\nB) xxx\nC) xxx\nD) xxx", "response": "ABCD"}
```

## 数据处理

```bash
python -m examples.cheyujie.mcq_data_preprocess
```

## 训练

### 单机

```bash
nohup ./examples/cheyujie/run_single_node.sh > ./train.log 2>&1 &
```

### 多机

```bash
./examples/cheyujie/run_multi_node.sh
```

### Megatron

1. 权重转换: `python scripts/converter_hf_to_mcore.py --hf_model_path $HF_MODEL_PATH --output_path $DIST_CKPT_PATH`

2. 训练: `./examples/cheyujie/run_megatron.sh`